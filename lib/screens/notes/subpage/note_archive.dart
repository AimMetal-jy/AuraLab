import 'package:flutter/material.dart';

class NoteArchivePage extends StatelessWidget {
  const NoteArchivePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('归档'),
    );
  }
}
