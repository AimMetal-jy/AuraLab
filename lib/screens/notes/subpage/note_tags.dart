import 'package:flutter/material.dart';

class NoteTagsPage extends StatelessWidget {
  const NoteTagsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('标签'),
    );
  }
}
